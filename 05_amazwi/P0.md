# P0 — WHAT WE DO NOW
### Allocated competition scope · priority-gated, not time-gated

> **Both of us read this, then start. No more planning.**
> The plan is done and it is good. The risk now is entirely execution.

---

**Nothing is built.** The only goal is a reliable, judge-only golden path. Follow the exit conditions in `plan/05_BUILD.md`; do not turn this allocation into a clock schedule.

---

## THE SPLIT — start immediately, in parallel

### 🔵 LETHABO — Experience lane

| # | Task | Done when | Routing |
|---|---|---|---|
| **L1** | **DONE.** Eight Setswana hero cards are native-reviewed and validator-clean in `content/cards_setswana.json` (sw-003 swapped pula→thipa mid-review; a currency/motto ambiguity and a validator-required second accepted-answer form both drove the swap). Expand to 30 only after P0 works. | Eight cards are cross-read and ready for the demo. | TOP/high for card judgement; BUILD for entry |
| **L2** | Wire `tokens.css` into the mockups so themes switch by `data-theme`. | One target screen switches without hex values in component CSS. | BUILD/medium |
| **L3** | Craft pass on the seven wireframe screens using `REFINEMENT_BRIEF.md` §2. | The five hero screens are coherent at mobile size. | BUILD/medium |
| **L4** | Put every error-state string in one reviewed language file. | Loading, permission, expired and provider states have plain copy. | BUILD/low |
| **L5** | Create the pitch-deck skeleton from real screenshots only. | Judge-only golden path and fallback are represented. | MID/medium |
| **L6** | Rehearse the open and close aloud. | Each teammate can run the whole demo alone. | Human rehearsal |

### 🟠 SBU — Platform lane

| # | Task | Done when | Routing |
|---|---|---|---|
| **S1** 🔴 | **DECIDED FOR THIS ACCOUNT.** The authenticated profile has no subscriptions, so Collections/Disbursements are not callable. Freeze the clearly labelled `DEMO_PROVIDER`; revisit only if the hackathon provisions a separate account. | Provider mode and proof/fallback are recorded in the receipt contract. | MID/high |
| **S2** | **DONE.** Eight isiZulu hero cards are native-reviewed and validator-clean in `content/cards_isizulu.json`. | Eight cards are cross-read and ready for the demo. | TOP/high for card judgement; BUILD for entry |
| **S3** | Write and test `is_correct` before implementation: Unicode/case/spacing normalisation, native-curated `accepted_answers`, and reviewed aliases only. No blanket prefix stripping or broad Levenshtein. | Unit cases cover accepted, rejected and reviewed-alias answers. | TOP/high |
| **S4** | **DONE.** The generic starter is tested; no AMAZWI-specific application code has been added. | The permission decision and starter boundary are recorded. | BUILD/low |
| **S5** | Implement schema/migrations from `02_TECH.md`, including accepted answers, violation evidence, `VOIDED`, `EXPIRED` and idempotent reward events. | Service invariants and migrations run locally. | MID/medium |
| **S6** | **DECIDED — no organiser email.** Keep pre-event permission and event-specific platform details labelled unknown; no product-specific implementation before event start. | Unknowns are recorded and do not block the labelled demo-provider fallback. | Human action |

Use the task tier and effort in `plan/12_MODEL_ROUTING.md`; select the locally available model that fits that tier. Do not treat a model label as a substitute for the stated acceptance condition.

### 🟣 BOTH — agree before work begins

- [ ] **Pick the theme** *(or ship the switcher and choose only after a target-device contrast check)*
- [ ] Confirm the role split as written
- [ ] Agree the kill rules in `plan/05_BUILD.md` §6 **now**, while nobody is invested

---

## THE THEME — you do not have to choose today

Lethabo asked to keep the ability to switch rather than commit. **That is now built:** `04_assets/themes/tokens.css` defines all five as `[data-theme]` blocks. Switching is one line:

```js
document.documentElement.dataset.theme = 'dusk';   // shweshwe | dusk | earth | ndebele | ink
```

**So the decision moves from "pick one now" to "choose after the target-device contrast check."** Build against tokens, never hex, and the choice stays open at zero cost.

**Three constraints that hold whatever gets picked:**
1. **`earth` ships regardless.** It is the Day theme and it is an accessibility requirement, not an aesthetic option.
2. **`dusk` is deck and splash only** — small text over a mesh gradient is a contrast fight you lose.
3. **`ndebele` is seasonal**, and if it is used the attribution is said out loud.

**For Sbu and Codex:** review the four grounds and pick three you would ship. Links below. Send the three back and Lethabo makes the final call from the shortlist.

**Sbu has Figma's free tier connected to Codex too** — so he can pull the same design-system variables (`04_assets/FIGMA.md`), reference the Foundations sheet, and push his own component work or screen refinements into the same file rather than a separate one. One file, two editors, both visible to each other. Coordinate in `BUILD_LOG.md` before either of you touches the same node — Figma's plugin API scripts are atomic per-call but not atomic *between* two people's calls, so a "who's touching Foundations right now" ping avoids overwriting each other's work.

---

## LINKS

| | |
|---|---|
| Theme directions | https://claude.ai/code/artifact/3b464f09-c6da-4e97-a831-729d1df53f0d |
| App mockups | https://claude.ai/code/artifact/889d9d01-823d-4a84-bd00-7d6e88007903 |
| Figma design system | https://www.figma.com/design/JPZuFmbhRh9fhkgBLxRymq |
| Token switcher | [`04_assets/themes/tokens.css`](../04_assets/themes/tokens.css) |

⚠️ **The two canvases are private.** Lethabo must share them from each page's own share menu.

---

## ECONOMICS FOR THE COMPETITION

Use `plan/03_BUSINESS.md` §§1–5 only: a sponsored language mission, a published speaker honorarium, points-only verifiers, an immediate ledger credit and provider cash-out at a confirmed viable threshold. Provider fees, acceptance, production verification cost, buyer demand and any redemption economics remain pilot measurements—not pitch figures.

---

## WHAT COMES AFTER L1–L6 / S1–S6

L1–L6 and S1–S6 above are the **pre-event allocation** — content and generic scaffolding, nothing that needs organiser approval or both of us in the same room. Once those are done, and once the organiser question (S6) is answered or the event starts, the real work is eight **Build Gates**, A→H, full detail in `plan/05_BUILD.md` §4. Each gate's exit condition must be true before the next one starts — do not skip ahead.

| Gate | Proves | Lethabo | Sbu |
|---|---|---|---|
| **A** | Shell runs on both laptops, deploys | Routes, tokens, Mini App/browser label | API health, DB, migrations, deploy |
| **B** | One tap reaches a receipt from **seeded** data | Clickable screens, loading/empty/error states | Seeded campaign/card/contribution/receipt |
| **C** | Consent is enforced, not just shown | Adult gate, language select, consent UX | Session adapter, versioned consent, enforcement |
| **D** | A real recording survives a refresh on another device | Permission, card, timer, waveform, record/retry | Upload, private playback URL, quality metrics |
| **E** | Two real verifiers make a clip eligible — MCQ can't | Free-text verifier flow, referee tap, reveal | Assignment, resolver, `VOIDED`/`EXPIRED` |
| **F** | Ledger can't double-pay, UI never overstates money | Wallet states, receipt, provider-mode label | Reward ledger, cash-out, reconciliation, retries |
| **G** | Funding leg traceable, every simulated part disclosed | "Fund a mission" screen | Collections sandbox or labelled seeded funding |
| **H** | Judge-only demo survives a reset, twice | Mobile pass, Impact Map, deck, fallback video | Log sanitisation, rate limits, recovery, backend script |

⚠️ **Gate B is a fixture, not the real path** — seeded data is inserted already resolved (`CORPUS_ELIGIBLE`, credited). The live resolver only exists from Gate E onward. Today's 8+8 cards feed Gate B's seed data directly, so they're not just for the pitch — they unblock the first gate that touches real screens.

**Everything from Gate A onward is application code.** That's exactly the line both of us are holding open on S6/S4 — Gates A–H are event-day (or post-approval) work, not tonight's.

---

## THE RULE FOR THIS BUILD

> **If it is not on this page, it does not get done today.**

Roadmap ideas — SASL, click trainer, Ask the Archive, dialect map, face capture, synthesis, redemption — are all recorded in `plan/11_EXPANSION.md`. **None of them is P0.** Reopening any of them weakens the golden path.

Log every block in `BUILD_LOG.md`. Pull before you start, push before you stop.
