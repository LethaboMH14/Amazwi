# P0 — WHAT WE DO NOW
### Allocated competition scope · priority-gated, not time-gated

> **Both of us read this, then start. No more planning.**
> The plan is done and it is good. The risk now is entirely execution.

---

**Competition implementation starts on-site when the event opens.** No written approval exists for pre-event product code, and S6 deliberately sends no organiser email. Everything produced before then — research, plans, reviewed language content, Figma work and static mockups — is preparation/reference only, not competition implementation, running-product evidence or a submission artifact. The event build must be completed by Sbu and Lethabo on-site. The goal remains a reliable, judge-only golden path; follow the exit conditions in `plan/05_BUILD.md` without turning this allocation into a clock schedule.

**⚠️ This line is contested, unresolved as of `05_amazwi/BUILD_LOG.md`'s `~23:10` entry.** Lethabo has started real AMAZWI-specific application code (`starter/frontend` routes/tokens/mode-label) on the stated basis that this rule is superseded. Sbu has not recorded acceptance. Resolve directly, not by whichever commit lands last.

---

## THE SPLIT — start immediately, in parallel

### 🔵 LETHABO — Experience lane

| # | Task | Done when | Routing |
|---|---|---|---|
| **L1** | **WAITING — structurally green, not fully language-approved.** All eight Setswana cards pass with zero errors and one explicit review warning naming the four replacement distractors: `moraka`, `jusi`, `ting` and `diphaphatha`. Lethabo must read them aloud and approve or replace them. Expand to 30 only after P0 works. | Lethabo records native approval of all four replacements in `BUILD_LOG.md`; validator returns zero errors and zero warnings. | TOP/high for card judgement; BUILD for entry |
| **L2/L3** | **PREPARATION COMPLETE.** Four Figma components, five hero-screen reference mockups and a token-switching reference demo exist. They are pre-event design preparation, not the competition frontend or proof that a build gate passed. Recreate/implement the chosen direction on-site. | Reference direction is coherent and ready to guide the on-site implementation; no mockup is presented as a running app. | Was BUILD/medium; completed as pre-event design preparation |
| **L4** | **DONE (Setswana side), Sbu's structural validator added in parallel.** All 10 states drafted, then corrected against a dictionary source (klipo→karolo, tekanyo→mogato, including a real class 3/4 grammar fix), then confirmed by Lethabo in `content/error_states.json`. Sbu independently added `content/validate_error_states.mjs` — all ten states pass structurally. isiZulu final aloud approval is Sbu's own open item, not blocking. | Loading, permission, expired and provider states have plain copy. | BUILD/low |
| **L5** | **PARTIAL — reference skeleton only.** `plan/14_DECK_SKELETON.md` scaffolds the ten-slide story, but the competition deck, real running-app screenshots, judge-only script and no-network fallback recording do not exist yet. Assemble them on-site from the actual build; do not use pre-event mockups as proof. | Every claim is backed by the live build or labelled as future work; actual screenshots, script and fallback recording exist on both laptops and a phone. | MID/medium |
| **L6** | Rehearse the open and close aloud. **Not the same as the sefofane exercise** — sefofane tested whether the game *mechanic* is playable in Setswana; L6 is rehearsing the actual demo *narration script* (opening line, live game narration, closing ask from `06_PITCH.md`). Real partial progress exists, but this is still open. | Each teammate can run the whole demo alone. | Human rehearsal |

**`LETHABO_NEXT_WORK.md` — CLOSED, 31 Aug 2026, items 1–6 done, item 7 half-done:**

1. ✅ Distractor-overlap warnings fixed (sw-004/005/007) — validator runs 0 errors, 0 warnings. Swaps are proposed pending Lethabo's own read-aloud confirmation, same bar as the original 8.
2. ✅ Was already done — Setswana error copy reviewed, `validate_error_states.mjs` passes.
3. ✅ Stale mockup content reconciled — SEFOFANE confirmed real (matches sw-001), a real bug fixed (ISITHUTHUTHU mislabelled "Setswana," replaced with kgomo across 3 files), League cut from the compiled judge-facing canvas per kill rules.
4. ✅ Five hero screens at craft-pass bar — Referee, Receipt and Archive/Impact-Map built to the v2 visual grammar, joining the existing Main/Recording/Understood.
5. ✅ Theme wiring — a real hero screen (`ThemeDemo.dc.html`) built entirely from `tokens.css` variables, three working `data-theme` switches, verified via real keyboard/DOM checks, not just eyeballed.
6. ✅ Accessibility/resilience evidence — full findings in `04_assets/mockups_v2/ACCESSIBILITY_EVIDENCE.md`. Found **and fixed same day**: every hero-screen CTA was keyboard-unreachable (styled divs, not buttons) — now real `<button>` elements across Main/Recording/Referee/Understood/ThemeDemo, re-verified with actual Tab presses. Setswana chip's under-sized touch target fixed too. Still open, correctly not patched into a mockup: fixed-390px canvases structurally can't pass a 200%-zoom reflow check — flagged as a hard requirement for the real frontend from Gate A onward.
7. ⚠️ Half-done — the judge-only demo script exists (`plan/15_DEMO_SCRIPT.md`), but the **no-network fallback recording cannot be produced yet**: it requires a real running golden path (Gate D/E), and recording the current static mockups and presenting that as "the fallback" would be exactly the kind of dishonesty the pitch contract rules out.

**L6 (rehearsal) is deferred** — Lethabo's explicit call, 31 Aug: "we will do later." `plan/15_DEMO_SCRIPT.md`'s rehearsal checklist exists now so there's something concrete to rehearse against when it happens.

**Sbu's status framing, current as of the governance reconciliation:** items 3–5 above produced reference assets but did not close a competition build gate — that only happens on-site, per `05_BUILD.md`. The live gaps are Lethabo's approval of the four replacement distractors, the finished deck/script/fallback recording and rehearsal (L6). This table is the current status summary; older queue wording is historical where it conflicts.

### 🟠 SBU — Platform lane

| # | Task | Done when | Routing |
|---|---|---|---|
| **S1** 🔴 | **DECIDED FOR THIS ACCOUNT.** The authenticated profile has no subscriptions, so Collections/Disbursements are not callable. Freeze the clearly labelled `DEMO_PROVIDER`; revisit only if the hackathon provisions a separate account. | Provider mode and proof/fallback are recorded in the receipt contract. | MID/high |
| **S2** | **DONE.** Eight isiZulu hero cards are native-reviewed and validator-clean in `content/cards_isizulu.json`. | Eight cards are cross-read and ready for the demo. | TOP/high for card judgement; BUILD for entry |
| **S3** | **DONE, cross-lane, pending Sbu's review.** `starter/backend/app/matching.py` implements `13_IS_CORRECT_SPEC.md`'s five-step pipeline exactly; no blanket prefix stripping, no edit-distance threshold added. `starter/backend/tests/test_matching.py` — 20 tests, including checks against the real hero-8 decks (every accepted answer matches itself, no distractor/blocked_word ever matches). Not wired into any endpoint. Built by Lethabo this session under the loosened lane rule — see `BUILD_LOG.md` 01 Sep ~00:05. | Unit cases cover accepted, rejected and reviewed-alias answers. | TOP/high |
| **S4** | **DONE (starter itself), superseded by S3/Gate A above.** The generic starter was tested clean before any AMAZWI-specific code existed. That is no longer the current state of the repo — Lethabo's Gate A frontend and this session's `matching.py` are both real AMAZWI-specific application code now, under the superseded pre-event-code rule and the loosened lane rule respectively. This row's original claim is historical, not current. | The permission decision and starter boundary are recorded. | BUILD/low |
| **S5** | **DONE, cross-lane, pending Sbu's review.** `starter/backend/app/models.py` implements every record from `02_TECH.md` §3 with CHECK/UNIQUE constraints from §4, §8 and `content/SCHEMA.md` enforced at the DB level. Real Alembic migration in `starter/backend/alembic/` — upgrade/downgrade/upgrade roundtrip verified against a real embedded PostgreSQL 16, which caught and fixed a real autogenerate bug (orphaned ENUM types breaking a second upgrade — exactly Gate H's "demo survives a reset, twice" failure mode). `app/ledger.py` implements the six §8 reward-ledger invariants. Backend CI (`.github/workflows/ci.yml`) hardened with a real `postgres:16` service container; test DB fixture now also supports an external `AMAZWI_TEST_DATABASE_URL` for local Postgres installs. **Extended beyond S5's original scope, same cross-lane basis:** `app/resolver.py` implements §5's assignment invariants and resolver branches. A later regression test corrected its transaction boundary: final contribution state, decision and reward now commit together, so a failed budget check leaves no terminal decision that blocks a retry. 40 backend tests added in this work, 0 mocks, all against real Postgres — full suite **62/62 passing**. Not built: MoMo adapter (§9), consent-derivation logic (resolver takes explicit `consent_active`/`audio_quality_passed` booleans rather than deriving them), random-cohort assignment selection (`create_assignment` takes an explicit verifier, doesn't pick one), any endpoint wiring. See `starter/backend/S5_README.md` and `HANDOVER_SBU.md`. | Service invariants and migrations run locally. | MID/medium |
| **S6** | **DECIDED — no organiser email.** Keep pre-event permission and event-specific platform details labelled unknown; no product-specific implementation before event start. | Unknowns are recorded and do not block the labelled demo-provider fallback. | Human action |

Use the task tier and effort in `plan/12_MODEL_ROUTING.md`; select the locally available model that fits that tier. Do not treat a model label as a substitute for the stated acceptance condition.

### 🟣 BOTH — agree before work begins

- [ ] **Pick the theme** *(or ship the switcher and choose only after a target-device contrast check)*
- [x] Confirm the role split as written
- [ ] Agree the kill rules in `plan/05_BUILD.md` §6 **now**, while nobody is invested

---

## THE THEME — you do not have to choose today

Lethabo asked to keep the ability to switch rather than commit. **A pre-event reference implementation exists:** `04_assets/themes/tokens.css` defines all five as `[data-theme]` blocks. The intended on-site implementation is one line:

```js
document.documentElement.dataset.theme = 'dusk';   // shweshwe | dusk | earth | ndebele | ink
```

**So the decision moves from "pick one now" to "choose after the target-device contrast check."** The pre-event theme demo proves a reference approach only. Wire the actual competition app against tokens on-site, never hex, and keep the choice open until the real target-device check.

**Three constraints that hold whatever gets picked:**
1. **`earth` ships regardless.** It is the Day theme and it is an accessibility requirement, not an aesthetic option.
2. **`dusk` is deck and splash only** — small text over a mesh gradient is a contrast fight you lose.
3. **`ndebele` is seasonal**, and if it is used the attribution is said out loud.

**For Sbu and Lethabo on-site:** review the grounds on the actual target device and pick three you would ship. Lethabo makes the final experience call from that shortlist.

The shared Figma file is a pre-event reference, not competition implementation. If the organiser rules permit the team to use it as reference on-site, Sbu and Lethabo should coordinate in `BUILD_LOG.md` before touching the same node and recreate the selected direction in the actual competition frontend. No outside assistant is part of the event team.

---

## LINKS

| | |
|---|---|
| Theme directions | https://claude.ai/code/artifact/3b464f09-c6da-4e97-a831-729d1df53f0d |
| App mockups | https://claude.ai/code/artifact/889d9d01-823d-4a84-bd00-7d6e88007903 |
| Figma design system | https://www.figma.com/design/JPZuFmbhRh9fhkgBLxRymq |
| Token switcher | [`04_assets/themes/tokens.css`](../04_assets/themes/tokens.css) |

⚠️ **The two canvases are private.** Lethabo must share them from each page's own share menu.

---

## ECONOMICS FOR THE COMPETITION

Use `plan/03_BUSINESS.md` §§1–5 only: a sponsored language mission, a published speaker honorarium, points-only verifiers, an immediate ledger credit and provider cash-out at a confirmed viable threshold. Provider fees, acceptance, production verification cost, buyer demand and any redemption economics remain pilot measurements—not pitch figures.

---

## WHAT COMES AFTER L1–L6 / S1–S6

L1–L6 and S1–S6 record **pre-event preparation status**. They do not close competition build gates and may guide the on-site team only to the extent the official rules permit. When the event opens, Sbu and Lethabo begin the eight Build Gates A→H in `plan/05_BUILD.md` §4. Each gate's exit condition must be true before the next one starts — do not skip ahead.

| Gate | Proves | Lethabo | Sbu |
|---|---|---|---|
| **A** | Shell runs on both laptops, deploys | **Lethabo's half started 31 Aug** — react-router-dom wired (patched to v7.18.3, not v6, after `npm audit` flagged real CVEs in v6 that a straight install would have shipped), `tokens.css` imported and rendering, an honest `ModeLabel` component (never claims a live MoMo host when running standalone), all covered by tests (12 passing) plus a sync test that fails if the two `tokens.css` copies ever drift. Verified live in a browser, not just `npm test` — console clean, theme applied, label correct. Still needed: real deploy target, Sbu's API-health half wired to a real backend (currently shows "unreachable" since none is running). | API health, DB, migrations, deploy |
| **B** | One tap reaches a receipt from **seeded** data | Clickable screens, loading/empty/error states | Seeded campaign/card/contribution/receipt |
| **C** | Consent is enforced, not just shown | Adult gate, language select, consent UX | Session adapter, versioned consent, enforcement |
| **D** | A real recording survives a refresh on another device | Permission, card, timer, waveform, record/retry | Upload, private playback URL, quality metrics |
| **E** | Two real verifiers make a clip eligible — MCQ can't | Free-text verifier flow, referee tap, reveal | Assignment, resolver, `VOIDED`/`EXPIRED` |
| **F** | Ledger can't double-pay, UI never overstates money | Wallet states, receipt, provider-mode label | Reward ledger, cash-out, reconciliation, retries |
| **G** | Funding leg traceable, every simulated part disclosed | "Fund a mission" screen | Collections sandbox or labelled seeded funding |
| **H** | Judge-only demo survives a reset, twice | Mobile pass, Impact Map, deck, fallback video | Log sanitisation, rate limits, recovery, backend script |

⚠️ **Gate B is a fixture, not the real path** — seeded data is inserted already resolved (`CORPUS_ELIGIBLE`, credited). The live resolver only exists from Gate E onward. Today's 8+8 cards feed Gate B's seed data directly, so they're not just for the pitch — they unblock the first gate that touches real screens.

**Everything from Gate A onward is competition application code.** With no written approval, Gates A–H begin on-site after the event opens. Pre-event plans and mockups may guide decisions only to the extent the organiser rules permit; they do not count as completed gates or submission evidence.

---

## THE RULE FOR THIS BUILD

> **If it is not on this page, it does not get done today.**

Roadmap ideas — SASL, click trainer, Ask the Archive, dialect map, face capture, synthesis, redemption — are all recorded in `plan/11_EXPANSION.md`. **None of them is P0.** Reopening any of them weakens the golden path.

Log every block in `BUILD_LOG.md`. Pull before you start, push before you stop.
