fastapi>=0.115
uvicorn[standard]>=0.30
pytest>=8.0
httpx>=0.27
sqlalchemy>=2.0
alembic>=1.14
psycopg2-binary>=2.9

# dev/test only -- embedded PostgreSQL for real migration/schema tests
# without requiring a Docker/system Postgres install (S5, cross-lane,
# pending Sbu's review -- see BUILD_LOG.md)
pgserver>=0.1.4
